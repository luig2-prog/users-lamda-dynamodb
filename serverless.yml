service: users-lamda-dynamodb

provider:
  name: aws
  runtime: nodejs16.x

plugins:
  - serverless-offline

functions:
  get-users:
    handler: handler.getUsers
    events:
      - http:
          path: users
          method: GET

